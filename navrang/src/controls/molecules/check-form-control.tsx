import React from "react";
import { ControlProps } from "../../types";

const CheckboxFormControl = (props: ControlProps) => {
    return <div>CheckboxFormControl</div>;
};

export default CheckboxFormControl;
